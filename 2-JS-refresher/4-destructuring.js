'use strict';

// Easily extract array elements or objects and store them into variables
const [a, b] = ['Hello', 'Matthijs']
console.log(a)
console.log(b)

const {name, age} = {name:'Mad Max', occupation:'driver', age: '28'}
console.log(name)
console.log(age)
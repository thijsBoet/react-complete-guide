// Can ommit the parenthesis when using one argument
const printMyName = name => {
  console.log(name);
}
printMyName('Thijs');

const multiply = number => number * 2
console.log(multiply(2))

export default multiply